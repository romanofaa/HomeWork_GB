# def url_parse(url):
#     _t_protocol, _, domain, *resource_adress = url.strip('/').split('/')
#     t_protocol = _t_protocol[:-1]
#     return  t_protocol, domain, resource_adress

# callback это когда мы передаем функцию как объект


# map(float, _x) пропускает последовательность через callback

def say_hello():
    print(name)

name = 'Ivan'
say_hello()
# Это плохой пример











